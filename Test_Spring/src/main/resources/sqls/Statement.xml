<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.min.edu.dao.IBoardDaoImpl">
	<select id="selectBoard" resultType="tDto">
		SELECT SEQ,ID,TITLE,CONTENT ,DELFLAG ,REGDATE 
		FROM EDUBOARD e 
		ORDER BY REGDATE DESC
	</select>

<insert id="insertBoard" parameterType="tDto">
	INSERT INTO HR.EDUBOARD
	(SEQ, ID, TITLE, 
	CONTENT, DELFLAG, REGDATE)
	VALUES(EDUBOARD_SEQ.NEXTVAL,#{id}, #{title}, 
			#{content}, 'N', SYSDATE)
</insert>

</mapper>
